name: Automated Retrain on Kaggle

on:
    workflow_dispatch:
    schedule:
        - cron: '33 7 * * *'

jobs:
    check_drift:
        runs-on: ubuntu-latest
        outputs:
            drift: ${{ steps.check.outputs.drift }}
        env:
            SPACE_URL: ${{ vars.SPACE_URL }}
        steps:
            - uses: actions/checkout@v4
            - uses: actions/setup-python@v4
              with: { python-version: '3.12' }
            - run: pip install requests
            - id: check
              run: echo "drift=$(python scripts/check_drift.py)" >> "$GITHUB_OUTPUT"



    update_secrets:
        needs: check_drift
        if: needs.check_drift.outputs.drift == 'true' || github.event_name != 'schedule'
        runs-on: ubuntu-latest
        env:
            KAGGLE_API_TOKEN: ${{ secrets.KAGGLE_TOKEN }}
            KAGGLE_USERNAME: ${{ vars.KAGGLE_USERNAME }}
            HF_TOKEN: ${{ secrets.HF_TOKEN}}
            HF_USER: ${{ vars.HF_USER}}
            SPACE_URL: ${{ vars.SPACE_URL }}
            MODEL_REPO: ${{ vars.MODEL_REPO}}
            METRICS_REPO: ${{ vars.METRICS_REPO}}
            SYNTH_DATA_REPO: ${{ vars.SYNTH_DATA_REPO}}
            SPACE_NAME: ${{ vars.SPACE_NAME}}
            COMPANY: ${{ vars.COMPANY}}
            COMPANY_DESC: ${{ vars.COMPANY_DESC}}
        steps:
            - uses: actions/checkout@v4
            - uses: actions/setup-python@v4
              with: { python-version: '3.12' }
            - run : pip install kaggle --upgrade
            - run: python scripts/update_kaggle_secrets.py


    retrain:
        needs: [check_drift, update_secrets]
        if: needs.check_drift.outputs.drift == 'true' || github.event_name != 'schedule'
        runs-on: ubuntu-latest
        env:
            KAGGLE_API_TOKEN: ${{ secrets.KAGGLE_TOKEN }}
            KAGGLE_USERNAME: ${{ vars.KAGGLE_USERNAME }}
        steps:
            - name: Checkout repository
              uses: actions/checkout@v4
            - uses: actions/setup-python@v4
              with: { python-version: '3.12' }
            - run: pip install kaggle --upgrade
            - run: python scripts/push_kaggle_kernel.py model sentiment-retraining


