    ENV Variables and secrets to be configured in local ENV
    and/or github repo -> settings -> Secrets and variables -> Actions
    vars for variables and secrets for secrets

COMPANY=company_name_to_monitor {vars}
COMPANY_DESC=company description feeded to LLM for domain specific data generation {vars}


    Create an huggingface account and write your account name                  
    Create an huggingface  token (finegrained or not)                     
    X  - Create an huggingface space for Docker -> now only PRO ACCOUNTS

    While I was completing the project pricing on huggingface changed, and now 
    only static pages are available for free, no Gradio or Docker
  

HF_TOKEN=a_write_read_access_token_generated_on_your_hf_profile {secrets}
HF_USER=your_username_on_huggingface {vars}
MODEL_REPO=name_for_your_model_on_huggingface {vars}
SPACE_NAME=name_for_your_space_on_huggingface {vars}
SPACE_URL=https://<HF_USER>-<SPACE_NAME>.hf.space {vars}
    (different syntax with - instead of /, retrieve from the space EMBED THIS SPACE options
    three dots near profile image on top right)
APP_PRED_URL=SPACE_URL/predict {vars}


SYNTH_DATA_REPO=name_for_repo_on_hf_generated_data_in_training {vars}
METRICS_REPO=name_training_results_dataset {vars}
POSTS_REPO=name_for_collected_posts_repo {vars}
(any name, autogenerated during pipeline)


    REGISTER ON GRAFANA CLOUD AND CREATE YOUR FREE STACK
    NOTE DOWN THE NAME OF THE STACK es.  https://yourstackname.grafana.net/  
    go to connections -> Add new connections -> search and select infinity -> add new connection on the top right
    ACCESS THE STACK AND IN THE LEFT MENU GO TO 
    ADMINISTRATION > USERS AND ACCESS > SERVICE ACCOUNTS
    CLICK ON CREATE ADD SERVICE ACCOUNT
    GIVE A NAME AND ADMIN ROLE 
    IN THE SERVICE ACCOUNT PAGE CREATE A TOKEN

for deploy:
GRAFANA_INSTANCE=yourstackname {vars}
for display in app:
GRAFANA_URL=https://yourstackname.grafana.net/ {vars} 
+ public-dashboards/numeric_specific_id if you set it public in UI (share externally option)
GRAFANA_TOKEN=token_generated_for_service_account {secrets}

    CREATE A KAGGLE ACCOUNT AND ON YOUR PROFILE
    PROCEED TO GENERATE API TOKEN  

KAGGLE_TOKEN=generated_token_on_kaggle {secrets}
KAGGLE_USERNAME=your_username_in_kaggle {vars}

    IF RUNNING AIRFLOW LOCALLY VIA docker-compose:

AIRFLOW_UID=1000 {local .env only} # richiesto su Linux - 
    in an hosted VM run in terminal id -u to access the correcr AIRFLOW_UID and paste it here
AIRFLOW_FERNET_KEY=generate with scripts/create_fernet_key_for_airflow.py {local .env only}
      


    REPLICATE ALL VARIABLE VALUES IN THE AIRFLOW VARIABLE WITH THE SAME NAME
    es. HF_TOKEN="<hf_token>" -> AIRFLOW_VAR_HF_TOKEN="<hf_token>"
    this will let Airflow automatically read them when the .env is mounted in the compose

    Use the template .env_clean for local or VM deploy 
    copy or rename it to .env and fill the values as above
